package homework09.experiment02;

/**
 * @author Cody Cortello and Nick Houle
 * @version 4/9/14.
 */
public class MyTestApplication {
    public static void main(String[] args) {
        // define the capacity to run the tests upon
        int capacity = 50;

        // run the test 'tries' number of times, where more tries produces more precise averages
        int tries = 50;
        for (int i = 0; i < tries; i++) {


            // create variables to store the average number of

            // run a test on a set of each capacity in the interval
//            for (int capacity = minCapacity; capacity<=maxCapacity; capacity++) {
            SimpleHashSet set = new SimpleHashSet(i);

        }
    }
}
